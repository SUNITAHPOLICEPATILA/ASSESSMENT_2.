#include <stdio.h>

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    if(num % 2 == 0)
        printf("%d is even.\n", num);
    else
        printf("%d is odd.\n", num);

    return 0;
}
My life at AUB has been full of learning and experiences.
echo "// Change in B1" >> program.c
echo "This is change in B1" >> life_at_aub.txt
